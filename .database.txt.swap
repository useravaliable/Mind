论命令式数据库的可能性
此数据库不用来存储数据，用来存储命令，由于不验证数据有效性，所有命令一并存储
数据格式需要清晰，必须包含命令，库，表，数据，数据的属性，用户，使用设备
将命令作为数据处理，避免了验证的时间，以空间换时间的方式管理数据库
定时清理无效命令，

优点：
执行效率快，减少冗余，复制数据更强，基本不需要解析命令就能存储。

缺点：
可读性差，优化难度大。

论单元格数据库的可能性
此数据库将数据转化为单元格，只支持写入读取删除，不支持修改，作用范围大，可按时间来控制数据库的运行模式。

优点：
安全性高，无权限问题发生。

缺点：
开发难度大，用处少

语言式数据库
将数据库所有的命令以及数据转化为语言的形式，增加高强度权限管理系统，获得大幅度的数据处理精度。

优点：
无安全问题，效率高

缺点：
开发简易，但用法少见。








